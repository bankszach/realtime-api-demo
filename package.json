{
  "name": "realtime-api-demo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "server:dev": "npm --prefix server run dev",
    "web:dev": "npm --prefix client run dev",
    "dev": "npm-run-all -p server:dev web:dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}

